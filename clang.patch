diff --git a/src/projects/compiler-rt/make/platform/clang_darwin.mk b/src/projects/compiler-rt/make/platform/clang_darwin.mk
index 56ba018..332500a 100644
--- a/src/projects/compiler-rt/make/platform/clang_darwin.mk
+++ b/src/projects/compiler-rt/make/platform/clang_darwin.mk
@@ -21,15 +21,9 @@ UniversalArchs.eprintf := i386
 Configs += 10.4
 UniversalArchs.10.4 := i386 x86_64
 
-# Configuration for targetting iOS, for some ARMv6 functions, which must be
-# in the same linkage unit, and for a couple of other functions that didn't
-# make it into libSystem.
-Configs += ios
-UniversalArchs.ios := i386 x86_64 armv6 armv7
-
 # Configuration for use with kernel/kexts.
 Configs += cc_kext
-UniversalArchs.cc_kext := armv6 armv7 i386 x86_64
+UniversalArchs.cc_kext := i386 x86_64
 
 ###
 
diff --git a/src/tools/clang/runtime/compiler-rt/Makefile b/src/tools/clang/runtime/compiler-rt/Makefile
index dcdd4f5..87a350a 100644
--- a/src/tools/clang/runtime/compiler-rt/Makefile
+++ b/src/tools/clang/runtime/compiler-rt/Makefile
@@ -42,7 +42,7 @@ ifeq ($(shell test -d $(COMPILERRT_SRC_ROOT) && echo OK),OK)
 RuntimeDirs :=
 ifeq ($(OS),Darwin)
 RuntimeDirs += darwin
-RuntimeLibrary.darwin.Configs = eprintf 10.4 ios cc_kext
+RuntimeLibrary.darwin.Configs = eprintf 10.4 cc_kext
 
 # On Darwin, fake Clang into using the iOS assembler (since compiler-rt wants to
 # build ARM bits).
